<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_learn"
            android:checked="true"
            android:icon="@drawable/ic_menu_learn"
            android:title="@string/nav_menu_learn" />
        <item
            android:id="@+id/nav_remember"
            android:icon="@drawable/ic_remember"
            android:title="@string/nav_menu_remember" />
    </group>

    <item android:title="@string/nav_menu_communicate">
        <menu>
            <item
                android:id="@+id/nav_share"
                android:icon="@drawable/ic_menu_share"
                android:title="@string/nav_menu_share" />
            <item
                android:id="@+id/nav_feedback"
                android:icon="@drawable/ic_menu_feedback"
                android:title="@string/nav_menu_feedback" />
        </menu>
    </item>

    <group android:id="@+id/about">
        <item
            android:id="@+id/nav_about"
            android:icon="@drawable/ic_menu_about"
            android:title="@string/nav_menu_about" />
    </group>
</menu>
